'use strict';

/* переменной b присвоили строку 'x'
цикл длится пока длина строки не достигнет 20
шаг конкатенация со строкой 'х'
*/

for (let b = 'x'; b.length <= 20; b += 'x') {
    console.log(b);
};
